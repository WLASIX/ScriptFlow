<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/topToolbar"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:background="#000000"
    app:contentInsetStart="0dp"
    app:contentInsetEnd="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="0dp">

        <!-- МЕНЮ -->
        <ImageView
            android:id="@+id/btnOptions"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:padding="12dp"
            android:src="@drawable/ic_options"
            android:clickable="true"
            android:tooltipText="@string/toolbar_menu"
            android:contentDescription="@string/toolbar_menu"
            android:background="?attr/selectableItemBackgroundBorderless" />

        <!-- ИМЯ ФАЙЛА -->
        <TextView
            android:id="@+id/txtFileName"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/default_filename"
            android:tooltipText="@string/toolbar_filename"
            android:contentDescription="@string/toolbar_filename"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <!-- ДЕЙСТВИЯ -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="end|center_vertical">

            <ImageView
                android:id="@+id/btnUndo"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="6dp"
                android:padding="12dp"
                android:src="@drawable/ic_undo"
                android:clickable="true"
                android:tooltipText="@string/toolbar_undo"
                android:contentDescription="@string/toolbar_undo"
                android:background="?attr/selectableItemBackgroundBorderless" />

            <ImageView
                android:id="@+id/btnRedo"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="6dp"
                android:padding="12dp"
                android:src="@drawable/ic_redo"
                android:clickable="true"
                android:tooltipText="@string/toolbar_redo"
                android:contentDescription="@string/toolbar_redo"
                android:background="?attr/selectableItemBackgroundBorderless" />

            <ImageView
                android:id="@+id/btnEyedropper"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="6dp"
                android:padding="12dp"
                android:src="@drawable/ic_eyedropper"
                android:clickable="true"
                android:tooltipText="@string/toolbar_eyedropper"
                android:contentDescription="@string/toolbar_eyedropper"
                android:background="?attr/selectableItemBackgroundBorderless" />

            <ImageView
                android:id="@+id/btnScreenshot"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="6dp"
                android:padding="12dp"
                android:src="@drawable/ic_screenshot"
                android:clickable="true"
                android:tooltipText="@string/toolbar_screenshot"
                android:contentDescription="@string/toolbar_screenshot"
                android:background="?attr/selectableItemBackgroundBorderless" />

            <ImageView
                android:id="@+id/btnSearch"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="6dp"
                android:padding="12dp"
                android:src="@drawable/ic_search"
                android:clickable="true"
                android:tooltipText="@string/toolbar_search"
                android:contentDescription="@string/toolbar_search"
                android:background="?attr/selectableItemBackgroundBorderless" />
        </LinearLayout>

    </LinearLayout>

</androidx.appcompat.widget.Toolbar>
